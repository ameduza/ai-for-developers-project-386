// Generated by Microsoft TypeSpec

import * as http from "node:http";

import * as serverRaw from "./operations/server-raw.js";

import { parseHeaderValueParameters } from "../helpers/header.js";

import {
  OwnerRoutes,
  BookingTypes,
  Bookings,
} from "../models/all/booking-service.js";

import {
  RouterOptions,
  createPolicyChain,
  createPolicyChainForRoute,
  HttpContext,
} from "../helpers/router.js";

export interface BookingServiceRouter {
  /**
   * Dispatches the request to the appropriate service based on the request path.
   *
   * This member function may be used directly as a handler for a Node HTTP server.
   *
   * @param request - The incoming HTTP request.
   * @param response - The outgoing HTTP response.
   */
  dispatch(request: http.IncomingMessage, response: http.ServerResponse): void;

  /**
   * An Express middleware function that dispatches the request to the appropriate service based on the request path.
   *
   * This member function may be used directly as an application-level middleware function in an Express app.
   *
   * If the router does not match a route, it will call the `next` middleware registered with the application,
   * so it is sensible to insert this middleware at the beginning of the middleware stack.
   *
   * @param req - The incoming HTTP request.
   * @param res - The outgoing HTTP response.
   * @param next - The next middleware function in the stack.
   */
  expressMiddleware(
    req: http.IncomingMessage,
    res: http.ServerResponse,
    next: () => void,
  ): void;
}

export function createBookingServiceRouter(
  ownerRoutes: OwnerRoutes,
  bookingTypes: BookingTypes,
  bookings: Bookings,
  options: RouterOptions<{
    ownerRoutes: OwnerRoutes<HttpContext>;
    bookingTypes: BookingTypes<HttpContext>;
    bookings: Bookings<HttpContext>;
  }> = {},
): BookingServiceRouter {
  const __onRequestNotFound_42 =
    options.onRequestNotFound ??
    ((ctx) => {
      ctx.response.statusCode = 404;
      ctx.response.setHeader("Content-Type", "text/plain");
      ctx.response.end("Not Found");
    });

  const __onInvalidRequest_43 =
    options.onInvalidRequest ??
    ((ctx, route, error) => {
      ctx.response.statusCode = 400;
      ctx.response.setHeader("Content-Type", "application/json");
      ctx.response.end(JSON.stringify({ error }));
    });

  const __onInternalError_44 =
    options.onInternalError ??
    ((ctx, error) => {
      ctx.response.statusCode = 500;
      ctx.response.setHeader("Content-Type", "text/plain");
      ctx.response.end("Internal server error.");
    });

  const __routePolicies_45 = options.routePolicies ?? {};

  const __routeHandlers_46 = {
    owner_routes_get_owner: createPolicyChainForRoute(
      "ownerRoutesGetOwnerDispatch",
      __routePolicies_45,
      "ownerRoutes",
      "getOwner",
      serverRaw.owner_routes_get_owner,
    ),
    owner_routes_list_booking_types: createPolicyChainForRoute(
      "ownerRoutesListBookingTypesDispatch",
      __routePolicies_45,
      "ownerRoutes",
      "listBookingTypes",
      serverRaw.owner_routes_list_booking_types,
    ),
    owner_routes_create_booking_type: createPolicyChainForRoute(
      "ownerRoutesCreateBookingTypeDispatch",
      __routePolicies_45,
      "ownerRoutes",
      "createBookingType",
      serverRaw.owner_routes_create_booking_type,
    ),
    owner_routes_list_upcoming_bookings: createPolicyChainForRoute(
      "ownerRoutesListUpcomingBookingsDispatch",
      __routePolicies_45,
      "ownerRoutes",
      "listUpcomingBookings",
      serverRaw.owner_routes_list_upcoming_bookings,
    ),
    booking_types_list_booking_types: createPolicyChainForRoute(
      "bookingTypesListBookingTypesDispatch",
      __routePolicies_45,
      "bookingTypes",
      "listBookingTypes",
      serverRaw.booking_types_list_booking_types,
    ),
    booking_types_list_slots: createPolicyChainForRoute(
      "bookingTypesListSlotsDispatch",
      __routePolicies_45,
      "bookingTypes",
      "listSlots",
      serverRaw.booking_types_list_slots,
    ),
    bookings_create: createPolicyChainForRoute(
      "bookingsCreateDispatch",
      __routePolicies_45,
      "bookings",
      "create",
      serverRaw.bookings_create,
    ),
    bookings_get: createPolicyChainForRoute(
      "bookingsGetDispatch",
      __routePolicies_45,
      "bookings",
      "get",
      serverRaw.bookings_get,
    ),
    bookings_delete: createPolicyChainForRoute(
      "bookingsDeleteDispatch",
      __routePolicies_45,
      "bookings",
      "delete",
      serverRaw.bookings_delete,
    ),
  } as const;

  const dispatch = createPolicyChain(
    "BookingServiceRouterDispatch",
    options.policies ?? [],
    async function (ctx, request, response) {
      const url = new URL(request.url!, `http://${request.headers.host}`);
      let path = url.pathname;

      if (path.length === 0) {
        return ctx.errorHandlers.onRequestNotFound(ctx);
      } else if (path.startsWith("/")) {
        path = path.slice(1);
        if (path.length === 0) {
          return ctx.errorHandlers.onRequestNotFound(ctx);
        } else if (path.startsWith("owner")) {
          path = path.slice(5);
          if (path.length === 0) {
            switch (request.method) {
              case "GET":
                return __routeHandlers_46.owner_routes_get_owner(
                  ctx,
                  ownerRoutes,
                );
              default:
                return ctx.errorHandlers.onRequestNotFound(ctx);
            }
          } else if (path.startsWith("/booking")) {
            path = path.slice(8);
            if (path.length === 0) {
              return ctx.errorHandlers.onRequestNotFound(ctx);
            } else if (path.startsWith("-types")) {
              path = path.slice(6);
              if (path.length === 0) {
                switch (request.method) {
                  case "GET":
                    return __routeHandlers_46.owner_routes_list_booking_types(
                      ctx,
                      ownerRoutes,
                    );
                  case "POST":
                    return __routeHandlers_46.owner_routes_create_booking_type(
                      ctx,
                      ownerRoutes,
                    );
                  default:
                    return ctx.errorHandlers.onRequestNotFound(ctx);
                }
              } else {
                return ctx.errorHandlers.onRequestNotFound(ctx);
              }
            } else if (path.startsWith("s")) {
              path = path.slice(1);
              if (path.length === 0) {
                switch (request.method) {
                  case "GET":
                    return __routeHandlers_46.owner_routes_list_upcoming_bookings(
                      ctx,
                      ownerRoutes,
                    );
                  default:
                    return ctx.errorHandlers.onRequestNotFound(ctx);
                }
              } else {
                return ctx.errorHandlers.onRequestNotFound(ctx);
              }
            }
          }
        } else if (path.startsWith("booking")) {
          path = path.slice(7);
          if (path.length === 0) {
            return ctx.errorHandlers.onRequestNotFound(ctx);
          } else if (path.startsWith("-types")) {
            path = path.slice(6);
            if (path.length === 0) {
              switch (request.method) {
                case "GET":
                  return __routeHandlers_46.booking_types_list_booking_types(
                    ctx,
                    bookingTypes,
                  );
                default:
                  return ctx.errorHandlers.onRequestNotFound(ctx);
              }
            } else if (path.startsWith("/")) {
              path = path.slice(1);
              if (path.length === 0) {
                return ctx.errorHandlers.onRequestNotFound(ctx);
              } else {
                let __id_idx = path.indexOf("/");
                __id_idx = __id_idx === -1 ? path.length : __id_idx;
                const id = path.slice(0, __id_idx);
                path = path.slice(__id_idx);
                if (path.length === 0) {
                  return ctx.errorHandlers.onRequestNotFound(ctx);
                } else if (path.startsWith("/slots")) {
                  path = path.slice(6);
                  if (path.length === 0) {
                    switch (request.method) {
                      case "GET":
                        return __routeHandlers_46.booking_types_list_slots(
                          ctx,
                          bookingTypes,
                          id,
                        );
                      default:
                        return ctx.errorHandlers.onRequestNotFound(ctx);
                    }
                  } else {
                    return ctx.errorHandlers.onRequestNotFound(ctx);
                  }
                }
              }
            }
          } else if (path.startsWith("s")) {
            path = path.slice(1);
            if (path.length === 0) {
              switch (request.method) {
                case "POST":
                  return __routeHandlers_46.bookings_create(ctx, bookings);
                default:
                  return ctx.errorHandlers.onRequestNotFound(ctx);
              }
            } else if (path.startsWith("/")) {
              path = path.slice(1);
              if (path.length === 0) {
                return ctx.errorHandlers.onRequestNotFound(ctx);
              } else {
                let __id_idx = path.indexOf("/");
                __id_idx = __id_idx === -1 ? path.length : __id_idx;
                const id = path.slice(0, __id_idx);
                path = path.slice(__id_idx);
                if (path.length === 0) {
                  switch (request.method) {
                    case "GET":
                      return __routeHandlers_46.bookings_get(ctx, bookings, id);
                    case "DELETE":
                      return __routeHandlers_46.bookings_delete(
                        ctx,
                        bookings,
                        id,
                      );
                    default:
                      return ctx.errorHandlers.onRequestNotFound(ctx);
                  }
                } else {
                  return ctx.errorHandlers.onRequestNotFound(ctx);
                }
              }
            }
          }
        }
      }

      return ctx.errorHandlers.onRequestNotFound(ctx);
    },
  );

  const __errorHandlers_47 = {
    onRequestNotFound: __onRequestNotFound_42,
    onInvalidRequest: __onInvalidRequest_43,
    onInternalError: __onInternalError_44,
  };
  return {
    dispatch(request, response) {
      const ctx = { request, response, errorHandlers: __errorHandlers_47 };
      return dispatch(ctx, request, response).catch((e) =>
        __onInternalError_44(ctx, e),
      );
    },
    expressMiddleware: function (request, response, next) {
      const ctx = { request, response, errorHandlers: __errorHandlers_47 };
      void dispatch(
        {
          request,
          response,
          errorHandlers: {
            ...__errorHandlers_47,
            onRequestNotFound: function () {
              next();
            },
          },
        },
        request,
        response,
      ).catch((e) => __onInternalError_44(ctx, e));
    },
  };
}
